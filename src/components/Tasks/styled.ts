import styled from '@emotion/styled';

export const Wrapper = styled.div`
  height: 100%;
`;

export const Advertising = styled.p`
  margin: 30px 0;
  text-align: center;
  font-size: ${({ theme }) => theme.fonts.l};
`;
